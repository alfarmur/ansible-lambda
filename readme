create server with key
lambda funstion explorer add ansible.pem file nad paste the pem key

ami with user data 

#!/bin/bash
yum update -y
yum install -y python3 
(Ansible needs Python on target nodes)
